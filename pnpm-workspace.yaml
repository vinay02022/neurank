# Single-package workspace. The `packages:` field is required so that
# `pnpm store path` (used by actions/setup-node@v4 to wire the pnpm
# cache in CI) doesn't fail with "packages field missing or empty".
packages:
  - "."

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
